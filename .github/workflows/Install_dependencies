- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    sudo apt-get update  # 시스템 패키지 업데이트
    sudo apt-get install -y python3-distutils python3-setuptools  # distutils와 setuptools 설치
    pip install numpy==1.23.5 pandas==1.5.3  # 호환되는 버전으로 설치
    pip install -r requirements.txt  # requirements.txt에 명시된 패키지 설치
