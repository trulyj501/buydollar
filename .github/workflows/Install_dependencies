- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    if [ -f requirements.txt ]; then
      pip install -r requirements.txt  # requirements.txt가 있으면 의존성 설치
    else
      echo "requirements.txt not found!"  # 파일이 없으면 오류 메시지 출력
    fi
