- name: Install setuptools, wheel and dependencies
  run: |
    python -m pip install --upgrade pip
    python -m pip install --upgrade setuptools wheel
    pip install -r requirements.txt  # dependencies 설치
